---
import Button from "./Atoms/Button.astro";
interface Props {
  title: string;
  body: string;
  href: string;
}

const { href, title, body } = Astro.props;
---

<li>
  <Button variant="outline" size="small"> example button </Button>
</li>
